from tkinter import * 

root = Tk()  # Creates a blank window using the tkinter function
theLabel = Label(root, text="Too easy") # assigning a variable equal to Label, meaning the variable will be a Label datatype
theLabel.pack() # "pack" places the Label anywhere it'll fit on the screen
root.mainloop() # runs programs
